import {BsSearch} from 'react-icons/bs'

import './index.css'

FilterGroup = props => {
  renderTypeOfEmployment = () => {
    const {typeOfEmploymentList} = props

    return typeOfEmploymentList.map(employmentType => {
      const {}
    })
  }
}
